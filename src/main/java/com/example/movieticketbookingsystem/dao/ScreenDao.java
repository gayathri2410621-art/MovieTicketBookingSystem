package com.example.movieticketbookingsystem.dao;

import com.example.movieticketbookingsystem.model.Screen;

public interface ScreenDao extends GenericDao<Screen, Long> {
    // Add specific screen queries if needed, e.g.:
    // List<Screen> findScreensByTheater(Long theaterId);
}
